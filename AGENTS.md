# Repository scope

This repository owns stable Java runtime utilities shared by Auth and KFE. It
must not contain service business rules, deployment manifests, credentials or
cross-repository protocol implementations.
