# Shared Java runtime

Build with `./gradlew check`. Keep only implementation-neutral utilities here.
Cross-repository DTOs and protocols belong to `kerosene-contracts`.

- [SPIFFE workload identity runtime](WORKLOAD_IDENTITY_RUNTIME.md)

Pending: audit every class and move service-specific code to Auth or KFE.
